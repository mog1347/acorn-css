{
    "name": "kernel-css",
    "version": "1.0.0",
    "description": "",
    "main": "index.js",
    "scripts": {
        "watch-test": "sass --style=expanded --source-map --stop-on-error --watch ./test:./test_dist",
        "sass-test": "sass --style=expanded --source-map --load-path=./test/modules --trace --stop-on-error ./test:./test_dist",
        "sass-src": "sass --style=expanded --source-map --trace --stop-on-error ./src:./dist"
    },
    "keywords": [
        "css",
        "framework"
    ],
    "author": "",
    "license": "ISC",
    "devDependencies": {
        "sass": "^1.24.4"
    }
}
